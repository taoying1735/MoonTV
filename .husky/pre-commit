#!/bin/sh
# 临时禁用 pre-commit hook 以解决 Windows bash 兼容性问题
# 开发者可以手动运行 'pnpm lint:pre-commit' 进行代码检查

echo "Pre-commit hook is disabled for Windows compatibility."
echo "Please run 'pnpm lint:pre-commit' manually before committing."
exit 0