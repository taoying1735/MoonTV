#!/bin/sh
# Windows 兼容性：临时禁用 commit-msg hook
# 开发者可以手动运行 commitlint 进行提交信息检查

echo "Commit-msg hook is disabled for Windows compatibility."
echo "Please ensure your commit message follows conventional commit format."
exit 0
